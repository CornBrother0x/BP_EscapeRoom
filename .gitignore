# Dependencies / build output
node_modules/
dist/

# OS / editor noise
.DS_Store

# Deploy / environment
.vercel/
.env
.env.*

# Confidential challenge brief — must NEVER enter git history
docs/prompt.md

# Microsoft-extracted reference images — local reference only, never shipped
# or published. aesthetic-notes.md in the same folder is tracked deliberately.
docs/references/*.png
docs/references/*.jpg
docs/references/*.jpeg
docs/references/*.gif
docs/references/*.bmp
